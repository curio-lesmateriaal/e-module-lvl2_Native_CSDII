---
id: 4
difficulty: 5
title: Geneste objecten en lijsten
type: external
description: Deserialiseer JSON met een array en een genest object.
task: Deserialiseer de "John Doe"-JSON uit de theorie naar de classes <code>Person</code> en <code>Address</code>.
environment: Visual Studio 2022 — je project Week6.Api
steps:
  - Zet de JSON uit paragraaf 4.6 als string-variabele in je code (of haal hem op van jsonplaceholder).
  - Maak de classes <code>Person</code> (met <code>List&lt;string&gt; Hobbies</code> en <code>Address Address</code>) en <code>Address</code>.
  - Deserialiseer met <code>Deserialize&lt;Person&gt;(json, options)</code>.
  - Print <code>person.Name</code>, het aantal hobby's en <code>person.Address.City</code>.
deliverables:
  - Person.cs, Address.cs en Program.cs
  - Screenshot van de output
hint: Een genest JSON-object wordt een eigen class; een JSON-array wordt een `List<T>`.
solution: |-
  ```csharp
  var person = JsonSerializer.Deserialize<Person>(personJson, options);
  Console.WriteLine($"{person.Name} heeft {person.Hobbies.Count} hobby's en woont in {person.Address.City}");
  ```
---
